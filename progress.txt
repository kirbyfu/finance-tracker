## Task 1: Schema - DONE

Added noise_phrases table + cleaned_description column to transactions.

- Added `uniqueIndex` import to drizzle-orm/sqlite-core
- Added `cleanedDescription: text('cleaned_description')` to transactions table
- Added `noisePhrases` table with id, phrase, sourceId, createdAt
- Added unique index on (phrase, sourceId)
- Added NoisePhrase/NewNoisePhrase type exports
- Ran db:push, populated 1090 existing rows with cleaned_description = description

Files changed:
- packages/server/src/db/schema.ts

All 34 tests pass. Types check.

## Task 2: Service - NoisePhrasesService - DONE

Created noise-phrases service with all required methods.

Methods implemented:
- list() - returns all noise phrases
- create(phrase, sourceId?) - inserts normalized phrase, recomputes cleaned_descriptions
- remove(id) - deletes phrase, recomputes cleaned_descriptions
- getSuggestions() - finds phrases appearing in 3+ categories via n-gram analysis
- recomputeCleanedDescriptions(sourceId?) - applies all noise filters to transactions
- cleanDescription(description, phrases) - applies filters to single description
- getPhrasesForSource(sourceId) - gets global + source-specific phrases

Key decisions:
- cleanDescription exported for testing (pure function)
- Noise filter application: case-insensitive regex replacement, then collapse whitespace
- getSuggestions uses n-grams 1-4 words, returns top 10 by category count
- recompute scoped by sourceId for efficiency

Files changed:
- packages/server/src/services/noise-phrases.ts (new)
- packages/server/src/services/noise-phrases.test.ts (new)

All 42 tests pass (8 new). Types check.
