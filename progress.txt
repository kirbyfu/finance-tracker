Task 1: Changed PAGE_SIZE from 50 to 100 in Transactions.tsx
Task 2: Added page size selector dropdown (100, 1000, All) near pagination controls. Pagination buttons hidden when "All" selected.
Task 3: Added "First Page" button (double chevron icon) before Previous button. Disabled when on page 0.
Task 4: Added useSearchParams to persist Monthly/Annual tab selection in URL via `view` param. Defaults to monthly.
Task 5: Added `year` and `month` URL params. State initialized from URL (defaults to current date). Selectors update URL on change.
Task 6: Replaced month Select dropdown with Popover containing 3x4 grid of month buttons. Added @radix-ui/react-popover and created Popover UI component. Current month highlighted with default variant.
Task 7: Moved year picker from page header to next to month picker in monthly tab. Also added year picker to annual tab so both views have their own date selection controls.
Task 8: Added prev/next navigation buttons (< >) on Reports page. Monthly view navigates by month (wrapping year). Annual view navigates by year. Next buttons disabled when at current month/year.
Task 9: Created multi-year comparison page at /reports/years. Shows table with years as columns, categories as rows (income first desc, then expenses by absolute value desc), and Net row. Added backend endpoint reports.multiYear. Added "Multi-Year Comparison" button on Reports page.
Task 1.1: Added "Create Rule" button (Wand2 icon) to uncategorized transaction rows only. Button shows when effectiveCategoryId is null. Files: packages/web/src/pages/Transactions.tsx. Note: Button UI-only; needs slide-out panel (1.2) to function.
Task 1.2: Created slide-out panel for rule creation. New components: Sheet (ui/sheet.tsx) using Radix Dialog with right-slide animation, CreateRulePanel (components/CreateRulePanel.tsx) with pattern/category/source form. Wired to Wand2 button in Transactions.tsx via ruleTransaction state. On save, creates rule and invalidates both rules and transactions caches.
Task 1.3: Auto-generate suggested regex pattern from transaction description. Added generateSuggestedPattern() function that escapes special chars, replaces digit sequences with \d+, collapses whitespace with \s+. useEffect auto-populates pattern field when panel opens. Files: packages/web/src/components/CreateRulePanel.tsx.
Task 1.4: Added inline "Create new category" option in category dropdown. Selecting "Create new category" shows inline form with name input and color picker grid. On create, category is added and auto-selected. Uses categories.create mutation. Files: packages/web/src/components/CreateRulePanel.tsx.
Task 1.5: Added preview showing "Would match N existing transactions" below pattern input. Uses rules.test query with debounced pattern. Shows loading state while testing. Files: packages/web/src/components/CreateRulePanel.tsx.
Task 1.6: Wired up single-flow save for rule creation. Click "Create Rule" now creates category (if new) then rule in one action using mutateAsync. Removed separate "Create Category" button. Button disabled state checks if creating new category (needs name) vs existing (needs selection). Files: packages/web/src/components/CreateRulePanel.tsx.
Task 2.1: Added selection state to transactions table with checkbox column. Created Checkbox UI component (ui/checkbox.tsx) using @radix-ui/react-checkbox. Added selectedIds state (Set<number>), toggleSelection/toggleSelectAll/clearSelection helpers, selectionTotal computed value. Header has select-all checkbox, rows have individual checkboxes. Selected rows highlighted via data-state="selected". Files: packages/web/src/pages/Transactions.tsx, packages/web/src/components/ui/checkbox.tsx.
Task 2.2: Implemented shift-click for range selection and ctrl/cmd-click for toggle. Added lastClickedIndexRef to track last clicked row. handleSelectionClick() handles modifier keys: shift+click selects range from last clicked to current, ctrl/cmd+click toggles single row without clearing others, plain click selects only that row. Row is clickable (cursor-pointer), checkbox clicks stopPropagation to keep simple toggle behavior. Files: packages/web/src/pages/Transactions.tsx.
Task 2.3: Added floating selection bar at bottom of page. Shows "{N} selected · ${total}" with Clear button. Uses fixed positioning, centered horizontally with rounded-full pill style. Only visible when selectedIds.size > 0. Uses existing selectionTotal and clearSelection. Files: packages/web/src/pages/Transactions.tsx.
