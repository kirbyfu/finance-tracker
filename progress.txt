## Task 1: Schema - DONE

Added noise_phrases table + cleaned_description column to transactions.

- Added `uniqueIndex` import to drizzle-orm/sqlite-core
- Added `cleanedDescription: text('cleaned_description')` to transactions table
- Added `noisePhrases` table with id, phrase, sourceId, createdAt
- Added unique index on (phrase, sourceId)
- Added NoisePhrase/NewNoisePhrase type exports
- Ran db:push, populated 1090 existing rows with cleaned_description = description

Files changed:
- packages/server/src/db/schema.ts

All 34 tests pass. Types check.

## Task 2: Service - NoisePhrasesService - DONE

Created noise-phrases service with all required methods.

Methods implemented:
- list() - returns all noise phrases
- create(phrase, sourceId?) - inserts normalized phrase, recomputes cleaned_descriptions
- remove(id) - deletes phrase, recomputes cleaned_descriptions
- getSuggestions() - finds phrases appearing in 3+ categories via n-gram analysis
- recomputeCleanedDescriptions(sourceId?) - applies all noise filters to transactions
- cleanDescription(description, phrases) - applies filters to single description
- getPhrasesForSource(sourceId) - gets global + source-specific phrases

Key decisions:
- cleanDescription exported for testing (pure function)
- Noise filter application: case-insensitive regex replacement, then collapse whitespace
- getSuggestions uses n-grams 1-4 words, returns top 10 by category count
- recompute scoped by sourceId for efficiency

Files changed:
- packages/server/src/services/noise-phrases.ts (new)
- packages/server/src/services/noise-phrases.test.ts (new)

All 42 tests pass (8 new). Types check.

## Task 3: Service - PatternSuggesterService (rewrite) - DONE

Rewrote pattern-suggester to use n-gram based suggestions.

New API:
- getSuggestions(transactionId?) - returns { patterns, detectedNoise }
- PatternSuggestion: { pattern, matchCount, sampleDescriptions }
- DetectedNoise: { phrase, categoryCount }
- getSuggestedPatterns() kept as legacy wrapper

Algorithm:
- Extract 1-4 word n-grams from target cleaned_description(s)
- Count matches across all cleaned_descriptions (case-insensitive)
- Filter to 2+ matches, sort by count, dedupe overlapping patterns
- Return top 5

Noise detection:
- Extract n-grams from raw description
- Find n-grams appearing in 3+ categories
- Return top 5 as detectedNoise

Removed old strategies:
- extractMerchantPrefix
- findCommonSubstrings
- normalizeDescription grouping
- dedupeOverlappingPatterns (replaced with simpler dedupe)

Files changed:
- packages/server/src/services/pattern-suggester.ts (rewritten)
- packages/server/src/services/pattern-suggester.test.ts (new)
- packages/server/src/services/noise-phrases.ts (removed unused imports)

All 50 tests pass (8 new). Types check.

## Task 4: API - noisePhrases router - DONE

Created tRPC router exposing NoisePhrasesService.

Endpoints:
- noisePhrases.list() - returns all noise phrases
- noisePhrases.create({ phrase, sourceId? }) - creates normalized phrase
- noisePhrases.delete({ id }) - removes phrase
- noisePhrases.getSuggestions() - returns noise phrase candidates

Added noisePhrasesRouter to appRouter in index.ts.

Files changed:
- packages/server/src/routers/noise-phrases.ts (new)
- packages/server/src/routers/index.ts

All 50 tests pass. Types check.

## Task 5: API - Update rules.getSuggestions - DONE

Updated rules.getSuggestions endpoint to use new PatternSuggesterService API.

Changes:
- Import getSuggestions instead of getSuggestedPatterns
- Accept optional transactionId param via z.object({ transactionId: z.number().optional() }).optional()
- Return { patterns, detectedNoise } structure
- Updated Rules.tsx to use suggestionsData.patterns instead of suggestions array

Files changed:
- packages/server/src/routers/rules.ts
- packages/web/src/pages/Rules.tsx

All 50 tests pass. Types check.

## Task 6: UI - Noise Filters section on Rules page - DONE

Added "Noise Filters" section to Rules.tsx.

Components:
- Card with Filter icon header, description
- Table: Phrase (code), Source (Global or source name), Delete button
- "Add Filter" Dialog: phrase input, optional source dropdown
- "Suggest Filters" toggle: shows collapsible suggestions panel
- Suggestions show phrase, category count, sample categories, accept button
- ConfirmDeleteDialog for delete confirmation

Mutations:
- createNoiseMutation invalidates list + suggestions
- deleteNoiseMutation invalidates list + suggestions

Key decisions:
- Placed section after Categorization Rules card
- Suggestions shown inline with toggle, not separate modal
- Reuses existing UI patterns (Dialog, Table, ConfirmDeleteDialog)

Files changed:
- packages/web/src/pages/Rules.tsx

All 50 tests pass. Types check.

## Task 7: UI - CreateRulePanel multiple suggestions - DONE

Updated CreateRulePanel to use new n-gram based suggestions API.

Changes:
- Removed generateSuggestedPattern() function (old regex escaping approach)
- Added rules.getSuggestions query with transactionId
- Added noisePhrases.create mutation for inline noise filter creation
- Added detected noise section (amber box) with phrase + category count + Add Filter buttons
- Added pattern suggestions section with selectable cards showing pattern, match count, sample descriptions
- Auto-selects first pattern when suggestions load
- Tracks added noise filters per-session to show "Added" state

Key decisions:
- Noise section appears above suggestions (more important context)
- Pattern cards are clickable, selected state shows with primary border
- Noise filters scoped to transaction's sourceId
- Filter Sparkles icon on suggestions header

Files changed:
- packages/web/src/components/CreateRulePanel.tsx

All 50 tests pass. Types check.

## Task 8: Hook - Compute cleaned_description on transaction insert - DONE

Updated transactions.import mutation to populate cleaned_description on insert.

Changes:
- Import getPhrasesForSource, cleanDescription from noise-phrases service
- Fetch noise phrases once before loop (global + source-specific)
- Compute cleanedDescription for each transaction using cleanDescription()
- Include cleanedDescription in insert values

Key decisions:
- Fetch phrases once per import batch for efficiency
- Use existing getPhrasesForSource() which returns global + source-specific phrases

Files changed:
- packages/server/src/routers/transactions.ts

All 50 tests pass. Types check.

## Task 9: Cleanup - Remove old pattern-suggester code - DONE

Removed legacy backwards-compatibility code no longer needed.

Changes:
- Removed getSuggestedPatterns() legacy wrapper function
- Removed import and test for getSuggestedPatterns (1 test removed)
- Old strategies (merchant prefix, common substring, normalized grouping) already removed in Task 3

Key decisions:
- Legacy wrapper was only used in tests, safe to remove
- No production code depended on it

Files changed:
- packages/server/src/services/pattern-suggester.ts
- packages/server/src/services/pattern-suggester.test.ts

All 49 tests pass. Types check.
